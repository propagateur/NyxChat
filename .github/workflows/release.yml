name: Build & Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: windows-latest
            label: Windows
            tor-script: powershell -ExecutionPolicy Bypass -File scripts/fetch-tor.ps1
          - os: macos-14
            label: macOS (Apple Silicon)
            tor-script: bash scripts/fetch-tor.sh
          - os: macos-13
            label: macOS (Intel)
            tor-script: bash scripts/fetch-tor.sh
          - os: ubuntu-22.04
            label: Linux
            tor-script: bash scripts/fetch-tor.sh

    name: Build (${{ matrix.label }})
    runs-on: ${{ matrix.os }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Rust (stable)
        uses: dtolnay/rust-toolchain@stable

      - name: Rust cache
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: src-tauri

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      # --- Linux dependencies ---
      - name: Install Linux system deps
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libwebkit2gtk-4.1-dev \
            libayatana-appindicator3-dev \
            librsvg2-dev \
            patchelf \
            libfuse2

      # --- Fetch Tor binary ---
      - name: Fetch Tor Expert Bundle
        run: ${{ matrix.tor-script }}

      # --- Copy WebView2Loader.dll for Windows ---
      - name: Prepare WebView2Loader (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          $dll = "src-tauri/vendor/WebView2Loader.dll"
          if (-Not (Test-Path $dll)) {
            Write-Host "WebView2Loader.dll not found in vendor, skipping (webviewInstallMode embedBootstrapper will handle it)"
          }

      # --- Install & Build ---
      - name: Install npm dependencies
        run: npm ci

      - name: Build Tauri app
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          # Run AppImage tooling without FUSE on CI runners (fixes "failed to run linuxdeploy")
          APPIMAGE_EXTRACT_AND_RUN: 1
        with:
          tagName: ${{ github.ref_name }}
          releaseName: 'NyxChat ${{ github.ref_name }}'
          releaseBody: |
            ## NyxChat ${{ github.ref_name }}

            End-to-end encrypted peer-to-peer messenger — no server, no account.

            ### Downloads

            | Platform | File |
            |----------|------|
            | 🪟 Windows | `NyxChat_*_x64-setup.exe` |
            | 🍎 macOS (Apple Silicon) | `NyxChat_*_aarch64.dmg` |
            | 🍎 macOS (Intel) | `NyxChat_*_x64.dmg` |
            | 🐧 Linux | `NyxChat_*_amd64.AppImage` / `.deb` |

            ### Changes
            - See the commits since the previous version for the full changelog.
          releaseDraft: false
          prerelease: false
